python change_gain.py low
python change_gain.py medium
python change_gain.py high
python change_gain.py max


python change_gain.py low

import subprocess
subprocess.run(["python", "change_gain.py", "medium"])

