Put the three files into your src/algorithms/ folder and make them executable (optional).

Ensure dependencies exist: requests (used in network_env). Install if needed:

pip3 install requests


Start your anomaly detector (if not already running):

python3 /home/generic/tatenda.Empower6G/office/DE-engine/src/algorithms/anomaly_detector.py


Start the agent manager:

python3 /home/generic/tatenda.Empower6G/office/DE-engine/src/algorithms/agent_manager.py
